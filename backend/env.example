# Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Flask Secret Key (generate a random string for production)
SECRET_KEY=your-secret-key-change-in-production

# Email Configuration
# See EMAIL_SETUP.md for detailed instructions for Gmail, Outlook, SendGrid, etc.
# 
# Gmail Example:
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=true
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password-here  # Use App Password, not regular password!
# MAIL_DEFAULT_SENDER=your-email@gmail.com
#
# Outlook Example:
# MAIL_SERVER=smtp-mail.outlook.com
# MAIL_PORT=587
# MAIL_USE_TLS=true
# MAIL_USERNAME=your-email@outlook.com
# MAIL_PASSWORD=your-app-password
# MAIL_DEFAULT_SENDER=your-email@outlook.com
#
# SendGrid Example (for production):
# MAIL_SERVER=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USE_TLS=true
# MAIL_USERNAME=apikey
# MAIL_PASSWORD=your-sendgrid-api-key
# MAIL_DEFAULT_SENDER=your-verified-email@yourdomain.com

# Quick Start - Uncomment and fill in for Gmail:
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=

# Frontend URL (for CORS in production)
# Leave as * for development, set to your frontend URL in production
FRONTEND_URL=*

# PostgreSQL Database Configuration
# Option 1: Use DATABASE_URL (recommended for Railway, Heroku, etc.)
# DATABASE_URL=postgresql://user:password@host:port/database
#
# Option 2: Use individual connection parameters (for local development)
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=electricity_logger
#
# Note: If DATABASE_URL is set, it will be used. Otherwise, individual parameters will be used.
DATABASE_URL=
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=electricity_logger

